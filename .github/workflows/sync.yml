name: Sync Image to Aliyun

on:
  push:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: 登录阿里云镜像仓库
        uses: docker/login-action@v3
        with:
          registry: registry.cn-beijing.aliyuncs.com
          username: ${{ secrets.ALIYUN_USERNAME }}
          password: ${{ secrets.ALIYUN_PASSWORD }}

      - name: 拉取并推送到阿里云
        run: |
          docker pull nginx:latest
          docker tag nginx:latest registry.cn-beijing.aliyuncs.com/wordpress-duanfeiyu/nginx:latest
          docker push registry.cn-beijing.aliyuncs.com/wordpress-duanfeiyu/nginx:latest
